<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.github.sixtrees.mapper.share.UrlMapper">

    <insert id="addUrl" parameterType="Url" useGeneratedKeys="false" keyProperty="id">
        INSERT into fs_url
        (real_param,fake_param,false_time)
        VALUES
        (#{realParam},#{fackParam},#{falseTime})

    </insert>

    <!--修改-->
</mapper>